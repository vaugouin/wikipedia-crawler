citizenphilsecrets.py
item
movie
person
serie
.env
__pycache__/
.claude/
archive/
# migrations/ is versioned since WIKIPEDIA-CRAWLER-019: these scripts operate on
# real production data, so they belong in the history and must reach the VPS by
# git pull like the rest of the code. __pycache__/ above already keeps the
# compiled artifacts out.

# Claude Code / agent local tooling
.agents/

# Diagnostic dumps and migration traces, produced at run time (usually on the VPS).
# They are outputs, never source. Listed here so `git add -A` cannot sweep them in:
# it already did once, shared-images.txt in commit 7f37bf6.
/shared-images.txt
/*.csv
